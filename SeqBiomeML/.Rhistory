"trained_model" = trained_model_caret,
"performance" = performance_tbl,
"roc_plot" = plot_out
)
)
}
### Function-1 ###
#' @noRd
#' Define cross-validation scheme and training parameters for nested analysis
define_cv_nested <- function(resamp_method, train_data, train_metadata, outcome_colname, hyperparams_list, perf_metric_function, class_probs, inner_fold, cv_times, groups, seed) {
# set seed
set.seed(seed)
# for cross-validation
if(resamp_method == "cv") {
if (!is.null(groups)) {
cvIndex <- create_grouped_k_innerfolds(
resamp_method,
groups,
inner_fold = inner_fold
)
message("Groups will be kept together in CV partitions")
} else {
cvIndex <- caret::createFolds(
factor(train_metadata %>% dplyr::pull(outcome_colname)),
k = inner_fold,
returnTrain = TRUE
)
message("Groups not provided for inner fold partitions")
}
message("Number of samples in training dataset in inner-loop range from ", paste0(round(range(sapply(cvIndex, length)/nrow(train_metadata)), 2), collapse = '-'))
seeds <- get_seeds_trainControl_nested(hyperparams_list=hyperparams_list, inner_fold=inner_fold, cv_times=1, ncol_train=ncol(train_data), nrow_train=1)
cv <- caret::trainControl(
method = resamp_method,
number = inner_fold,
repeats = NA,
index = cvIndex,
returnResamp = "final",
classProbs = class_probs,
summaryFunction = perf_metric_function,
indexFinal = NULL,
savePredictions = TRUE,
seeds = seeds
)
}
# for repeated cross-validation
if(resamp_method == "repeatedcv") {
if (!is.null(groups)) {
cvIndex <- create_grouped_k_innerfolds(
resamp_method,
groups,
inner_fold = inner_fold,
cv_times = cv_times
)
message("Groups will be kept together in CV partitions")
} else {
cvIndex <- caret::createFolds(
factor(train_metadata %>% dplyr::pull(outcome_colname)),
k = inner_fold,
returnTrain = TRUE
)
message("Groups not provided for inner fold partitions")
}
message("Number of samples in training dataset in inner-loop range from ", paste0(round(range(sapply(cvIndex, length)/nrow(train_metadata)), 2), collapse = '-'))
seeds <- get_seeds_trainControl_nested(hyperparams_list=hyperparams_list, inner_fold=inner_fold, cv_times=cv_times, ncol_train=ncol(train_data), nrow_train=1)
cv <- caret::trainControl(
method = resamp_method,
number = inner_fold,
repeats = cv_times,
index = cvIndex,
returnResamp = "final",
classProbs = class_probs,
summaryFunction = perf_metric_function,
indexFinal = NULL,
savePredictions = TRUE,
seeds = seeds
)
}
# for loocv
if(resamp_method == "LOOCV") {
if (!is.null(groups)) {
### Get index in and index out
index_test <- vector(mode = "list", length(unique(groups)))
index_train <- vector(mode = "list", length(unique(groups)))
j=0
for (i in unique(groups)) {
j = (j + 1)
index_test[[j]] <- which(groups %in% i)
index_train[[j]] <- which(!groups %in% i)
}
message("Groups will be kept together in fold partitions")
seeds <- get_seeds_trainControl_nested(hyperparams_list=hyperparams_list, inner_fold=1, cv_times=1, ncol_train=ncol(train_data), nrow_train=length(unique(groups)))
} else {
### Get index in and index out
index_test <- vector(mode = "list", length(row.names(train_data)))
index_train <- vector(mode = "list", length(row.names(train_data)))
j=0
for (i in row.names(train_data)) {
j = (j + 1)
index_test[[j]] <- which(row.names(train_data) %in% i)
index_train[[j]] <- which(!row.names(train_data) %in% i)
}
message("Groups not provided for inner fold partitions")
seeds <- get_seeds_trainControl_nested(hyperparams_list=hyperparams_list, inner_fold=1, cv_times=1, ncol_train=ncol(train_data), nrow_train=nrow(train_data))
}
cv <- caret::trainControl(
method = resamp_method,
number = NA,
repeats = NA,
index = index_train,
indexOut = index_test,
returnResamp = "final",
classProbs = class_probs,
summaryFunction = perf_metric_function,
indexFinal = NULL,
savePredictions = TRUE,
seeds = seeds
)
}
return(cv)
}
out <- run_ml_nestedcv(comp_data, con_ath, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
library(mRMRe)
library(glmnet)
library(Boruta)
S_meta[S_meta$ParticipantType != "control", 2] = "Athlete"
comp_data <- S_rsv[rownames(S_rsv) %in% S_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, S_meta, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
lapply(out[[1]], function(x) x[[6]]) # to look at performance
lapply(out[[1]], function(x) x[[7]]) # to look at plots
View(Meta_data)
View(Meta_meta)
comp_data <- S_species[rownames(S_species) %in% S_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, S_meta, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
comp_data <- S_species[rownames(S_species) %in% S_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, S_meta, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
View(S_species)
comp_data
any(is.na(comp_data))
any(is.character(comp_data))
any(!is.numeric(comp_data))
which(!is.numeric(comp_data))
which(!is.numeric(comp_data))
comp_data
!is.numeric(comp_data))
!is.numeric(comp_data)
comp_data == !is.numeric(comp_data)
comp_data[!is.numeric(comp_data)]
comp_data[is.numeric(comp_data)]
is.numeric(comp_data)
comp_data <- S_species[rownames(S_species) %in% S_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, S_meta, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
impute_zeros(comp_data, list("clr", "pseudo", 1))
impute_zeros(comp_data, list("clr", "CZM", 1))
impute_zeros(comp_data, list("clr", "pseudo", 1))
impute_zeros(comp_data, list("clr", "pseudo", "min"))
apply(comp_data, 1, is.numeric)
any(apply(comp_data, 1, is.numeric))
# Check if each column is numeric
is_numeric_column <- sapply(comp_data, is.numeric)
# Identify non-numeric columns
non_numeric_columns <- names(comp_data)[!is_numeric_column]
print(non_numeric_columns)
comp_data[["Bacteroides vulgatus"]]
comp_data[1, 1]
is.numeric(comp_data[1, 1])
any(apply(comp_data, 1, is.numeric))
any(apply(comp_data, 1, as.numeric))
apply(comp_data, 1, as.numeric))
apply(comp_data, 1, as.numeric)
test = apply(comp_data, 1, as.numeric)
test = as.numeric(comp_data)
t(test)
in_path = "C:/Users/jacko/Documents/SeqBiomeML/SeqBiomeML/R/"
# in_path = "C:/Users/jacko/Downloads/SeqBiomeML_latest/SeqBiomeML/R/"
file_path <- list.files(in_path, full.names = TRUE)
for(f_path in file_path) { source(f_path) }
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
dataset = as.data.frame(t(dataset)) # switches samples to rows and features to columns
dataset = dataset[rownames(dataset) %in% metadata$SampleID, ] # selects only samples that should be in the analysis
dataset = as.data.frame(apply(dataset, 1, as.numeric)) # sets all values as numeric
return(dataset = dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
new_Meta_data <- lapply(Meta_data, function(x) format_data(x, Meta_meta))
S_rsv = new_S_data[["S_rsv"]]
S_species = new_S_data[["S_species"]]
S_genus = new_S_data[["S_genus"]]
Meta_species = new_Meta_data[["Meta_species"]]
Meta_EC = new_Meta_data[["Meta_EC"]]
# Read DESCRIPTION file to extract Imports
description_file <- "C:/Users/jacko/Downloads/SeqBiomeML_latest/SeqBiomeML/DESCRIPTION"
description <- readLines(description_file)
pkg = description[13:28]
pkg = pkg[-15]
for (i in pkg) {
x = sub(",", "", i)
y = trimws(x)
if (!requireNamespace(y, quietly = TRUE)) {
install.packages(y)
}
library(y, character.only = T)
}
library(mRMRe)
library(glmnet)
library(Boruta)
S_meta[S_meta$ParticipantType != "control", 2] = "Athlete"
comp_data <- S_species[rownames(S_species) %in% S_meta$SampleID, ]
comp_data
rownames(S_species)
rownames(S_rsv)
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
View(S_rsv)
rownames(S_rsv) = S_rsv$taxa # renames the rows as the taxa column
S_rsv = S_rsv[, -1] # removes taxa column
S_rsv = as.data.frame(t(S_rsv)) # switches samples to rows and features to columns
S_rsv = S_rsv[rownames(S_rsv) %in% S_meta$SampleID, ] # selects only samples that should be in the analysis
S_rsv = as.data.frame(apply(S_rsv, 1, as.numeric))
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
rownames(S_rsv) = S_rsv$taxa # renames the rows as the taxa column
S_rsv = S_rsv[, -1] # removes taxa column
S_rsv = as.data.frame(apply(S_rsv, 1, as.numeric))
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
rownames(S_rsv) = S_rsv$taxa # renames the rows as the taxa column
S_rsv = S_rsv[, -1] # removes taxa column
S_rsv1 = as.data.frame(apply(S_rsv, 1, as.numeric))
View(S_rsv1)
rownames(S_rsv) = S_rsv$taxa # renames the rows as the taxa column
S_rsv = S_rsv[, -1] # removes taxa column
S_rsv1 = as.data.frame(apply(S_rsv, 1, as.numeric))
rownames(S_rsv1) = rownames(S_rsv)
rownames(S_rsv1) = colnames(S_rsv)
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
rownames(S_rsv) = S_rsv$taxa # renames the rows as the taxa column
S_rsv = S_rsv[, -1] # removes taxa column
S_rsv1 = as.data.frame(apply(S_rsv, 1, as.numeric))
rownames(S_rsv1) = colnames(S_rsv)
S_rsv = as.data.frame(t(S_rsv)) # switches samples to rows and features to columns
in_path = "C:/Users/jacko/Documents/SeqBiomeML/SeqBiomeML/R/"
# in_path = "C:/Users/jacko/Downloads/SeqBiomeML_latest/SeqBiomeML/R/"
file_path <- list.files(in_path, full.names = TRUE)
for(f_path in file_path) { source(f_path) }
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
new_dataset = as.data.frame(apply(dataset, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(new_dataset) = colnames(dataset) # renames the rows
new_dataset = as.data.frame(apply(new_dataset, 1, as.numeric)) # sets all values as numeric
return(dataset = new_dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
new_Meta_data <- lapply(Meta_data, function(x) format_data(x, Meta_meta))
S_rsv = new_S_data[["S_rsv"]]
S_species = new_S_data[["S_species"]]
S_genus = new_S_data[["S_genus"]]
Meta_species = new_Meta_data[["Meta_species"]]
Meta_EC = new_Meta_data[["Meta_EC"]]
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
new_dataset = as.data.frame(apply(dataset, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(new_dataset) = colnames(dataset) # renames the rows
new_dataset = as.data.frame(apply(new_dataset, 1, as.numeric)) # sets all values as numeric
return(dataset = new_dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
format_data(S_rsv, S_meta)
test = format_data(S_rsv, S_meta)
format_data(S_rsv, S_meta)
rownames(S_rsv) = S_rsv$taxa # renames the rows as the taxa column
S_rsv = S_rsv[, -1] # removes taxa column
S_rsv1 = as.data.frame(apply(S_rsv, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(S_rsv1) = colnames(S_rsv) # renames the rows
S_rsv1 = S_rsv1[rownames(S_rsv1) %in% S_meta$SampleID, ] # selects only samples that should be in the analysis
in_path = "C:/Users/jacko/Documents/SeqBiomeML/SeqBiomeML/R/"
# in_path = "C:/Users/jacko/Downloads/SeqBiomeML_latest/SeqBiomeML/R/"
file_path <- list.files(in_path, full.names = TRUE)
for(f_path in file_path) { source(f_path) }
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
new_dataset = as.data.frame(apply(dataset, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(new_dataset) = colnames(dataset) # renames the rows
new_dataset = new_dataseta[rownames(new_dataset) %in% S_meta$SampleID, ] # sets all values as numeric
return(dataset = new_dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
new_dataset = as.data.frame(apply(dataset, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(new_dataset) = colnames(dataset) # renames the rows
new_dataset = new_dataset[rownames(new_dataset) %in% S_meta$SampleID, ] # sets all values as numeric
return(dataset = new_dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
new_Meta_data <- lapply(Meta_data, function(x) format_data(x, Meta_meta))
S_rsv = new_S_data[["S_rsv"]]
S_species = new_S_data[["S_species"]]
S_genus = new_S_data[["S_genus"]]
Meta_species = new_Meta_data[["Meta_species"]]
Meta_EC = new_Meta_data[["Meta_EC"]]
test = format_data(S_rsv, S_meta)
test = format_data(S_rsv, S_meta)
test = format_data(S_rsv, S_meta)
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
test = format_data(S_rsv, S_meta)
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
test = format_data(S_species, S_meta)
test = format_data(S_genus, S_meta)
test = format_data(S_species, S_meta)
View(test)
any(apply(test, 1, is.na))
any(is.na(test))
test = format_data(S_species, S_meta)
any(is.na(test))
test = format_data(S_species, S_meta)
summary(S_species)
summary(test)
summary(s_species)
summary(S_species)
is.na(test)
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
new_Meta_data <- lapply(Meta_data, function(x) format_data(x, Meta_meta))
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
new_dataset = as.data.frame(apply(dataset, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(new_dataset) = colnames(dataset) # renames the rows
new_dataset = new_dataset[rownames(new_dataset) %in% S_meta$SampleID, ] # sets all values as numeric
return(dataset = new_dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
new_Meta_data <- lapply(Meta_data, function(x) format_data(x, Meta_meta))
S_rsv = new_S_data[["S_rsv"]]
S_species = new_S_data[["S_species"]]
S_genus = new_S_data[["S_genus"]]
Meta_species = new_Meta_data[["Meta_species"]]
Meta_EC = new_Meta_data[["Meta_EC"]]
library(mRMRe)
library(glmnet)
library(Boruta)
S_meta[S_meta$ParticipantType != "control", 2] = "Athlete"
comp_data <- S_species[rownames(S_species) %in% S_meta$SampleID, ]
comp_data
rownames(S_rsv)
out <- run_ml_nestedcv(comp_data, S_meta, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
comp_data <- S_genus[rownames(S_genus) %in% S_meta$SampleID, ]
comp_data
out <- run_ml_nestedcv(comp_data, S_meta, method = "rf", outcome_colname = "ParticipantType", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
View(Meta_species)
sum(Meta_species[, 1])
sum(as.numeric(Meta_species[, 1]))
as.numeric(2)
as.numeric(0.2)
sum(as.numeric(Meta_species[, 1]))
as.numeric(Meta_species[, 1])
sum(as.numeric(Meta_species[, 2]))
sum(as.numeric(Meta_species[, 3]))
sum(as.numeric(Meta_species[, 4]))
sum(as.numeric(Meta_species[, 5]))
rownames(Meta_species) = Meta_species$taxa # renames the rows as the taxa column
Meta_species = Meta_species[, -1] # removes taxa column
new_dataset = as.data.frame(apply(Meta_species, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
View(new_dataset)
rownames(new_dataset) = colnames(Meta_species) # renames the rows
new_dataset = new_dataseta[rownames(new_dataset) %in% Meta_meta$SampleID, ] # sets all values as numeric
new_dataset = new_dataset[rownames(new_dataset) %in% Meta_meta$SampleID, ] # sets all values as numeric
# Reading in the Data
S_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_metadata.csv")
S_rsv = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_rsv.csv")
S_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
S_genus = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/16S_species.csv")
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_species = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_species.csv")
Meta_EC = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_EC.csv")
# Metadata
S_meta <- S_meta[S_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
# Formats the count table so that they are applicable with the functions
format_data <- function(dataset, metadata) {
rownames(dataset) = dataset$taxa # renames the rows as the taxa column
dataset = dataset[, -1] # removes taxa column
new_dataset = as.data.frame(apply(dataset, 1, as.numeric)) # switches samples to rows and features to columns and makes them numeric
rownames(new_dataset) = colnames(dataset) # renames the rows
new_dataset = new_dataset[rownames(new_dataset) %in% metadata$SampleID, ] # sets all values as numeric
return(dataset = new_dataset)
}
# Count Data
S_data <- list(S_rsv = S_rsv, S_species = S_species, S_genus = S_genus)
Meta_data <- list(Meta_species = Meta_species, Meta_EC = Meta_EC)
new_S_data <- lapply(S_data, function(x) format_data(x, S_meta))
new_Meta_data <- lapply(Meta_data, function(x) format_data(x, Meta_meta))
S_rsv = new_S_data[["S_rsv"]]
S_species = new_S_data[["S_species"]]
S_genus = new_S_data[["S_genus"]]
Meta_species = new_Meta_data[["Meta_species"]]
Meta_EC = new_Meta_data[["Meta_EC"]]
View(Meta_meta)
Meta_meta[Meta_meta$Group != "control", 3] = "Athlete"
comp_data <- Meta_species[rownames(Meta_species) %in% Meta_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, Meta_meta, method = "rf", outcome_colname = "Group", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
Meta_meta = read.csv("C:/Users/jacko/Documents/SeqBiomeML/Data/Meta_metadata.csv")
Meta_meta <- Meta_meta[, 1:14] # removes all blank columns
Meta_meta <- Meta_meta[Meta_meta$INCLUDE == c("YES"), ] # Only selects samples that are supposed to be included for analysis
Meta_meta[Meta_meta$Group != "Control", 3] = "Athlete"
comp_data <- Meta_species[rownames(Meta_species) %in% Meta_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, Meta_meta, method = "rf", outcome_colname = "Group", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
lapply(out[[1]], function(x) x[[6]]) # to look at performance
lapply(out[[1]], function(x) x[[7]]) # to look at plots
comp_data <- Meta_EC[rownames(Meta_EC) %in% Meta_meta$SampleID, ]
out <- run_ml_nestedcv(comp_data, Meta_meta, method = "rf", outcome_colname = "Group", group_colname = "SubjectID", scale_method = list("clr", "pseudo", 1), preprocess_methods = c("nzv"), corrFunList = list(corr_method = "spearman", corr_thresh = 1, group_neg_corr = TRUE), filter_features = T, filterFunList = list(test = "mrmr_filter", n = 100), outer_folds = NULL, resamp_method = "cv", n_outer_folds = 5, n_inner_folds = 5, cv_times = 10, training_frac = 0.80, hyperparameters = NULL, cross_val = NULL, perf_metric_function = NULL, perf_metric_name = NULL, group_partitions = NULL, class_weight = FALSE, find_feature_importance = FALSE, feature_importance_method = "permutation", jobs = 1, threads = 1, seed = 0)
lapply(out[[1]], function(x) x[[6]]) # to look at performance
lapply(out[[1]], function(x) x[[7]]) # to look at plots
